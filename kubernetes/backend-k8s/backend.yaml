apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: final-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
      tier: backend
      component: api
  template:
    metadata:
      labels:
        app: backend
        tier: backend
        component: api
    spec:
      initContainers:
        - name: wait-for-postgres
          image: busybox
          command:
            - sh
            - -c
            - until nc -z vaultedkube-postgres.cghcymykc28f.us-east-1.rds.amazonaws.com 5432; do echo waiting for db; sleep 5; done
      containers:
        - name: backend
          image: 296674251987.dkr.ecr.us-east-1.amazonaws.com/vaultedkube-backend:latest
          ports:
            - containerPort: 5000
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: postgres-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 10
            periodSeconds: 10
